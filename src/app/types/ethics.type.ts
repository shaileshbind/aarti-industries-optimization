import { ImageProps } from "./global.type";

export type EthicsAndCodeProps = {
    data: {
      description: string;
      image: ImageProps;
      mobImage: ImageProps;
      heading: string;
    };
  };