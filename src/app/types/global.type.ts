export type ImageProps = {
  url: string;
  alternativeText: string;
};

export type ButtonProps = {
  title: string;
  link: string;
};

export type ValueProps = {
  value: string;
  description: string;
};
